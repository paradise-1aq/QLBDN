@model QLBDN.Models.MatchEvent
@{
    Layout = "~/Views/Shared/_layout.cshtml";
    ViewData["Title"] = "Xóa sự kiện";

    string icon = Model.EventType switch {
        "Goal" => "⚽",
        "Own Goal" => "🥅",
        "Yellow Card" => "🟨",
        "Red Card" => "🟥",
        "Substitution" => "🔄",
        "Penalty Miss" => "❌",
        "Penalty Goal" => "⚽",
        "VAR Review" => "📺",
        _ => "📌"
    };
}

@section Styles {
<style>
    .delete-card {
        max-width: 700px;
        margin: 0 auto;
        padding: 2rem;
        background: white;
        border-radius: 18px;
        box-shadow: 0 8px 25px rgba(0,0,0,0.08);
        border: 2px solid #fee2e2;
    }

    .delete-title {
        font-size: 1.8rem;
        font-weight: 800;
        color: #dc2626;
        text-align: center;
        margin-bottom: 1.5rem;
    }

    .delete-icon {
        text-align: center;
        font-size: 4rem;
        margin-bottom: 1rem;
    }

    .delete-info {
        background: #fef2f2;
        padding: 1rem;
        border-radius: 12px;
        margin-bottom: 2rem;
        color: #7f1d1d;
        font-weight: 600;
        border: 1px solid #fecaca;
    }

    .btn-delete {
        background: #dc2626;
        color: white;
        padding: 1rem;
        width: 100%;
        border-radius: 12px;
        font-weight: 700;
        border: none;
        cursor: pointer;
    }

    .btn-back {
        display: block;
        margin-top: 1rem;
        text-align: center;
        color: #1e293b;
        font-weight: 600;
    }
</style>
}

<div class="delete-card fade-in">
    
    <div class="delete-icon">@icon</div>

    <div class="delete-title">Bạn có chắc muốn xóa sự kiện này?</div>

    <div class="delete-info">
        @Model.EventType — @Model.Player?.FullName  
        <br />
        @Model.DateTime?.ToString("HH:mm • dd/MM/yyyy")
    </div>

    <form asp-action="Delete">
        <input type="hidden" asp-for="EventID" />
        <button class="btn-delete">🗑 Xóa ngay</button>
    </form>

    <a asp-action="Index" asp-route-matchId="@Model.MatchID" class="btn-back">← Quay lại</a>
</div>
