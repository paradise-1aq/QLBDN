@model IEnumerable<QLBDN.Models.MatchEvent>

@{
    Layout = "~/Views/Shared/_layout.cshtml";
    ViewData["Title"] = "Sự kiện trận đấu";

    var match = ViewBag.Match as QLBDN.Models.Match;
    var matchList = ViewBag.MatchList as List<QLBDN.Models.Match>;
}

@section Styles {
<style>
    .events-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 1rem;
    }

    /* ---------------- HEADER ---------------- */
    .page-header {
        background: white;
        border-radius: 16px;
        padding: 2rem;
        margin-bottom: 2rem;
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.04);
        border: 1px solid #f1f5f9;
    }

    .header-top {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 1.5rem;
        gap: 1rem;
    }

    .header-left {
        flex: 1;
    }

    .page-title {
        font-size: 2rem;
        font-weight: 800;
        color: #059669;
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .page-subtitle {
        color: #64748b;
        font-size: 0.95rem;
    }

    .btn-add {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        background: linear-gradient(135deg, #34d399, #10b981);
        padding: 0.75rem 1.75rem;
        border-radius: 12px;
        color: white;
        text-decoration: none;
        font-weight: 700;
        font-size: 0.95rem;
        box-shadow: 0 4px 12px rgba(16, 185, 129, 0.35);
        transition: all 0.3s;
        white-space: nowrap;
    }

    .btn-add:hover {
        background: linear-gradient(135deg, #10b981, #059669);
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(16, 185, 129, 0.45);
    }

    /* ---------------- MATCH INFO CARD ---------------- */
    .match-info-card {
        background: linear-gradient(135deg, #ecfdf5, #d1fae5);
        border-radius: 12px;
        padding: 1.5rem;
        border: 1px solid #a7f3d0;
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1.5rem;
    }

    .info-item {
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }

    .info-icon {
        width: 45px;
        height: 45px;
        border-radius: 10px;
        background: white;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.25rem;
        box-shadow: 0 2px 8px rgba(16, 185, 129, 0.2);
        flex-shrink: 0;
    }

    .info-content {
        flex: 1;
        min-width: 0;
    }

    .info-label {
        font-size: 0.75rem;
        color: #047857;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-bottom: 0.25rem;
    }

    .info-value {
        font-size: 0.95rem;
        color: #0f172a;
        font-weight: 700;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    /* ---------------- STATS BAR ---------------- */
    .stats-bar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background: white;
        border-radius: 12px;
        padding: 1rem 1.5rem;
        margin-bottom: 2rem;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
        border: 1px solid #f1f5f9;
    }

    .stats-count {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 0.9rem;
        color: #64748b;
    }

    .stats-count strong {
        color: #10b981;
        font-size: 1.25rem;
        font-weight: 800;
    }

    .event-legend {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .legend-item {
        display: flex;
        align-items: center;
        gap: 0.4rem;
        font-size: 0.85rem;
        color: #64748b;
        padding: 0.35rem 0.75rem;
        background: #f8fafc;
        border-radius: 8px;
    }

    .legend-icon {
        font-size: 1rem;
    }

    /* ---------------- EVENTS GRID ---------------- */
    .events-grid {
        display: grid;
        gap: 1rem;
    }

    .event-card {
        background: white;
        border-radius: 16px;
        padding: 1.5rem;
        border: 1px solid #f1f5f9;
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.04);
        transition: all 0.3s;
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 1.5rem;
    }

    .event-card:hover {
        transform: translateY(-4px);
        box-shadow: 0 8px 24px rgba(16, 185, 129, 0.2);
        border-color: #6ee7b7;
    }

    .event-left {
        display: flex;
        align-items: center;
        gap: 1.25rem;
        flex: 1;
        min-width: 0;
    }

    .event-icon {
        width: 55px;
        height: 55px;
        border-radius: 14px;
        background: linear-gradient(135deg, #d1fae5, #a7f3d0);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.75rem;
        flex-shrink: 0;
        box-shadow: 0 4px 12px rgba(16, 185, 129, 0.2);
        transition: all 0.3s;
    }

    .event-card:hover .event-icon {
        transform: scale(1.1) rotate(5deg);
    }

    .event-details {
        flex: 1;
        min-width: 0;
    }

    .event-type {
        display: inline-flex;
        align-items: center;
        padding: 0.35rem 0.85rem;
        border-radius: 8px;
        background: #ecfdf5;
        color: #047857;
        font-weight: 700;
        font-size: 0.8rem;
        margin-bottom: 0.5rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .event-player {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 1rem;
        color: #0f172a;
        font-weight: 600;
        margin-bottom: 0.4rem;
    }

    .event-time {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: #64748b;
        font-size: 0.875rem;
    }

    .event-actions {
        display: flex;
        gap: 0.5rem;
        flex-shrink: 0;
    }

    .btn-action {
        width: 38px;
        height: 38px;
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        text-decoration: none;
        font-size: 1rem;
        transition: all 0.3s;
        border: 1.5px solid;
        cursor: pointer;
    }

    .btn-view {
        background: #f1f5f9;
        color: #475569;
        border-color: #e2e8f0;
    }

    .btn-view:hover {
        background: #e2e8f0;
        border-color: #cbd5e1;
        transform: translateY(-2px);
    }

    .btn-edit {
        background: linear-gradient(135deg, #34d399, #10b981);
        color: white;
        border-color: #10b981;
    }

    .btn-edit:hover {
        background: linear-gradient(135deg, #10b981, #059669);
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
    }

    .btn-delete {
        background: #fee2e2;
        color: #dc2626;
        border-color: #fca5a5;
    }

    .btn-delete:hover {
        background: #fecaca;
        border-color: #f87171;
        transform: translateY(-2px);
    }

    /* ---------------- EMPTY STATE ---------------- */
    .empty-state {
        text-align: center;
        padding: 4rem 2rem;
        background: white;
        border-radius: 16px;
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.04);
        border: 1px solid #f1f5f9;
    }

    .empty-icon {
        font-size: 4rem;
        margin-bottom: 1rem;
        opacity: 0.5;
    }

    .empty-text {
        color: #64748b;
        font-size: 1.1rem;
        margin-bottom: 1.5rem;
    }

    /* ---------------- ANIMATIONS ---------------- */
    .fade-in {
        animation: fadeIn 0.5s ease;
    }

    @@keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* ---------------- RESPONSIVE ---------------- */
    @@media (max-width: 768px) {
        .events-container {
            padding: 0 0.75rem;
        }

        .page-header {
            padding: 1.5rem;
        }

        .header-top {
            flex-direction: column;
            align-items: stretch;
        }

        .page-title {
            font-size: 1.6rem;
        }

        .btn-add {
            width: 100%;
            justify-content: center;
        }

        .match-info-card {
            grid-template-columns: 1fr;
            gap: 1rem;
        }

        .stats-bar {
            flex-direction: column;
            gap: 1rem;
            align-items: stretch;
        }

        .event-legend {
            justify-content: center;
        }

        .event-card {
            flex-direction: column;
            align-items: stretch;
        }

        .event-left {
            flex-direction: row;
        }

        .event-actions {
            justify-content: center;
        }
    }
</style>
}

<div class="events-container">

    <!-- PAGE HEADER -->
    <div class="page-header fade-in">
        <div class="header-top">
            <div class="header-left">
                <h2 class="page-title">
                    🎬 Sự kiện trận đấu
                </h2>
                <p class="page-subtitle">
                    Quản lý và theo dõi diễn biến trận đấu
                </p>
            </div>

            @if (match != null)
            {
                <a class="btn-add" asp-action="Create" asp-route-matchId="@match.MatchId">
                    ➕ Thêm sự kiện
                </a>
            }
        </div>

        <!-- 🔥 BỘ LỌC TRẬN ĐẤU -->
        <div style="margin-top: 1rem;">
            <label style="font-weight: 700; color:#047857;">Chọn trận đấu:</label>
            <select id="matchFilter"
                    style="padding:.7rem 1rem; border-radius:10px; border:2px solid #d1d5db;">
                <option value="">-- Hiển thị tất cả sự kiện --</option>

                @foreach (var m in matchList)
                {
                    @if (match != null && m.MatchId == match.MatchId)
                    {
                        <option value="@m.MatchId" selected>
                            @($"#{m.MatchId} • {m.Stadium} • {m.DateTime:dd/MM HH:mm}")
                        </option>
                    }
                    else
                    {
                        <option value="@m.MatchId">
                            @($"#{m.MatchId} • {m.Stadium} • {m.DateTime:dd/MM HH:mm}")
                        </option>
                    }
                }
            </select>
        </div>

        <!-- MATCH INFO -->
        @if (match != null)
        {
            <div class="match-info-card" style="margin-top:1.5rem;">
                <div class="info-item">
                    <div class="info-icon">🆔</div>
                    <div class="info-content">
                        <div class="info-label">Mã trận đấu</div>
                        <div class="info-value">@match.MatchId</div>
                    </div>
                </div>

                <div class="info-item">
                    <div class="info-icon">🏟️</div>
                    <div class="info-content">
                        <div class="info-label">Sân vận động</div>
                        <div class="info-value">@match.Stadium</div>
                    </div>
                </div>

                <div class="info-item">
                    <div class="info-icon">📅</div>
                    <div class="info-content">
                        <div class="info-label">Thời gian</div>
                        <div class="info-value">@match.DateTime?.ToString("dd/MM/yyyy HH:mm")</div>
                    </div>
                </div>
            </div>
        }
    </div>

    <!-- STATS BAR -->
    <div class="stats-bar fade-in">
        <div class="stats-count">
            Tổng số sự kiện:
            <strong>@Model.Count()</strong>
        </div>

        <div class="event-legend">
            <div class="legend-item">⚽ Bàn thắng</div>
            <div class="legend-item">🟨 Thẻ vàng</div>
            <div class="legend-item">🟥 Thẻ đỏ</div>
            <div class="legend-item">🔄 Thay người</div>
        </div>
    </div>

    <!-- EVENTS LIST -->
    @if (!Model.Any())
    {
        <div class="empty-state fade-in">
            <div class="empty-icon">🎬</div>
            <div class="empty-text">
                @(match != null ? "Chưa có sự kiện nào trong trận đấu này" : "Hãy chọn một trận đấu để xem sự kiện")
            </div>

            @if (match != null)
            {
                <a class="btn-add" asp-action="Create" asp-route-matchId="@match.MatchId">
                    ➕ Thêm sự kiện đầu tiên
                </a>
            }
        </div>
    }
    else
    {
        <div class="events-grid">
            @foreach (var e in Model)
            {
                <div class="event-card fade-in">
                    <div class="event-left">
                        <div class="event-icon">
                            @switch(e.EventType)
                            {
                                case "Goal": <span>⚽</span>; break;
                                case "Own Goal": <span>🥅</span>; break;
                                case "Yellow Card": <span>🟨</span>; break;
                                case "Red Card": <span>🟥</span>; break;
                                case "Substitution": <span>🔄</span>; break;
                                case "Penalty Goal": <span>⚽</span>; break;
                                case "Penalty Miss": <span>❌</span>; break;
                                case "VAR Review": <span>📺</span>; break;
                                default: <span>📌</span>; break;
                            }
                        </div>

                        <div class="event-details">
                            <div class="event-type">@e.EventType</div>

                            <div class="event-player">
                                👤 <strong>@e.Player?.FullName</strong>
                            </div>

                            <div class="event-time">
                                ⏱ @e.DateTime?.ToString("HH:mm • dd/MM/yyyy")
                            </div>
                        </div>
                    </div>

                    <div class="event-actions">
                        <a class="btn-action btn-view" asp-action="Details" asp-route-id="@e.EventID">👁</a>
                        <a class="btn-action btn-edit" asp-action="Edit" asp-route-id="@e.EventID">✏️</a>
                        <a class="btn-action btn-delete" asp-action="Delete" asp-route-id="@e.EventID">🗑</a>
                    </div>
                </div>
            }
        </div>
    }
</div>

@section Scripts {
<script>
    document.getElementById("matchFilter").addEventListener("change", function () {
        const matchId = this.value;

        if (matchId === "") {
            window.location.href = "/MatchEvents"; // Hiển thị tất cả
        } else {
            window.location.href = "/MatchEvents?matchId=" + matchId;
        }
    });
</script>
}